package com.trana.patient.ui.home

// TODO: Implement HomeViewModel.
// Responsibilities:
//   - Fetch and expose patient profile from Firebase /users/{uid}
//   - Reverse-geocode current GPS coordinates to a human-readable address
//   - Trigger SOS dispatch: write dispatch record to Firebase /dispatches
//   - Expose dispatch state as StateFlow (IDLE / DISPATCHING / DISPATCHED)
